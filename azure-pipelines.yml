# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'Pay-As-You-Go (a83bdf64-2d6e-4ea0-a6df-ecadf9c278b7)'
    subscriptionId: 'a83bdf64-2d6e-4ea0-a6df-ecadf9c278b7'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'AzureRmPipeline-rg'
    location: 'East US'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://github.com/Githubxwyuan/hello-github-actions'
    overrideParameters: '-projectName Integrating External Source Control with Azure Pipelines'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate'
pool:
  vmImage: ubuntu-latest

steps:
- script: 
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
